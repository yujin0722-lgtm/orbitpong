# Orbit Pong：他AI向け引継書

## 1. プロジェクト概要

Orbit Pongは、円形フィールドで人間とCPUが対戦する、1画面完結型の
ブラウザゲームである。古いオシロスコープや初期iPodのおまけゲームを
思わせる、暗色・蛍光色・最小限のUIを採用している。

公開版：

https://orbitpong.su-udon.com

このリポジトリは、公開中のChatGPT Sites版をGitHub保存用に書き出した
ソーススナップショットである。

ユーザーは非エンジニアで、自分でコードを書く予定はない。修正を依頼されたAIは、
コード断片だけを提示してユーザーに編集を求めるのではなく、可能な限り自ら
ファイルを修正し、ビルドまたは動作確認まで行うこと。

---

## 2. ゲームの核

- 円周の下半分を人間、上半分をCPUが担当する
- ボールを自分のバーで返し、相手側で失点させる
- 先に5点取った側が勝利
- 返球が続くほどボールとバーが加速する
- バーの端で返すと打球角度が変わる
- バーを動かしながら返すと、移動方向が打球へ強く加算される
- 長いラリーでは緑→黄色→赤へ色調が変化する

最も重要な魅力は、スマートフォンの横スワイプが円弧上の滑らかな移動へ
変換される操作感である。機能追加よりも、この気持ちよさを優先する。

---

## 3. 操作

### スマートフォン

- ゲーム領域上で指を左右へ動かす
- Pointer Eventsで操作する
- `touch-action: none`により、プレイ中の意図しないスクロールを防止する
- 指の位置そのものではなく、Canvas中心から見た角度をバーの目標角度にする
- バーは目標角度へ滑らかに追従する

### PC

- マウス移動
- 左右矢印キー
- A／Dキー
- SpaceまたはPで一時停止

---

## 4. 現在のゲーム進行

`Phase`は次の5状態で管理される。

| 状態 | 意味 |
|---|---|
| `ready` | 初期画面 |
| `playing` | ラリー中 |
| `paused` | 一時停止 |
| `point` | 得点後の待機 |
| `finished` | どちらかが5点取得 |

得点後は自動再開しない。

1. `YOUR POINT`または`CPU POINT`を表示
2. `READY — CLICK TO SERVE`を表示
3. ゲーム画面をクリックすると次のサーブを開始

この一呼吸はスマートフォンでの遊びやすさに重要なので、明確な要望なしに
自動再開へ戻さないこと。

---

## 5. 主要ファイル

### `app/page.tsx`

ゲームのほぼすべてを担当する。

- ゲーム状態
- Canvas描画
- ボール物理
- バーの移動
- CPU
- 衝突判定
- 得点
- 色遷移
- 効果音
- React UI

ゲームの調整は、まずこのファイル冒頭の定数を確認する。

### `app/globals.css`

- コンソール外観
- Canvasの配置
- スコア表示
- メッセージ
- 難易度ボタン
- スマートフォン／低い画面向けレスポンシブ対応

### `src/main.tsx`、`index.html`

静的Vite版のブラウザーエントリーポイントとHTMLメタデータ。

### `vite.config.ts`

GitHub PagesのプロジェクトURLでも動くよう、アセットを相対パスで出力する設定。
ゲーム調整だけなら通常は変更不要。

### `.github/workflows/deploy-pages.yml`

`main`ブランチへのpush時にテスト、ビルド、GitHub Pagesへの公開を行う。

---

## 6. 主要パラメータ

`app/page.tsx`冒頭にまとめてある。

| 定数 | 現在値 | 役割 |
|---|---:|---|
| `SIZE` | 720 | Canvas内部座標 |
| `RADIUS` | 285 | 円周半径 |
| `PADDLE_HALF` | 0.24 | バーの半角幅（ラジアン） |
| `WIN_SCORE` | 5 | 勝利点 |
| `BASE_BALL_SPEED` | 255 | サーブ直後の速度 |
| `MAX_BALL_SPEED` | 485 | ボール速度上限 |
| `BALL_ACCELERATION` | 1.0525 | 返球ごとの速度倍率 |
| `PADDLE_ACCELERATION` | 0.027 | 返球ごとのバー加速量 |
| `MAX_PADDLE_MULTIPLIER` | 1.6 | バー速度上限倍率 |
| `PADDLE_SPIN_FACTOR` | 42 | バー運動が打球へ与える強さ |
| `MAX_PADDLE_SPIN` | 170 | 運動スピン上限 |
| `COLOR_TRANSITION_HITS` | 52 | 緑から赤までの返球数 |

CPUの基本追従速度：

| 難易度 | 値 |
|---|---:|
| EASY | 1.15 |
| NORMAL | 1.8 |
| HARD | 2.55 |

複数の難易度要因を同時に変えず、まず一つの値だけを変更して実機確認すること。

---

## 7. 衝突と打球方向

ボールが円周へ達した時点で、その角度と担当バーの中心角度を比較する。

```text
angleDistance(hitAngle, paddle) <= PADDLE_HALF + 0.055
```

バーの見た目より少し広い判定を採用している。これは厳密な公平性よりも、
スマートフォンでの気持ちよさを優先したものなので、理由なく狭くしない。

返球時は次の3要素から方向を作る。

1. 円周の法線に対する基本反射
2. バー中心からの衝突位置による接線方向の力
3. バーの角速度による接線方向の力

この3番目により、バーを振りながら打つ操作に意味が生まれている。

---

## 8. 加速と色

返球ごとにボール速度を約5.25%上げ、最大速度で制限する。バーも返球数に応じて
加速し、基準の1.6倍で上限となる。

色は実際のボール速度ではなく、ラリーの返球数を基準に変化させる。

- 0付近：緑
- 中間：黄色
- 52返球：赤

当初は色変化が速すぎたため、現在は長いラリーでゆっくり変化する設定になっている。
色の変化だけを調整するときは、ボールの物理速度を変更しないこと。

---

## 9. レスポンシブ設計

ゲーム領域の大きさはCSS変数`--scope-size`で決まる。

```css
min(660px, calc(100vw - 64px), calc(100dvh - 290px))
```

画面幅だけでなく画面高も考慮している。これにより、公開URLでCanvasだけが巨大化して
表示崩れする問題を修正した。

- 720px以下：説明文を隠し、操作部を簡略化
- 高さ720px以下：縦方向を圧縮
- 高さ520px以下：横長レイアウト

CanvasのCSS表示サイズを変更しても、内部座標`SIZE = 720`は通常変更しない。

---

## 10. デザイン方針

- 暗い筐体
- 円形の蛍光スクリーン
- Courier系の小さな技術表示
- 白に近いプレイヤーバー
- 色変化するCPUバー、ボール残光、境界線
- 装飾を増やしすぎない

「昔のiPodに最初から入っていた、説明不要で触ると分かるゲーム」という印象が
完成形の基準である。

新しいパネル、通貨、実績、アイテム、複雑なチュートリアルなどは、このゲームの
ミニマムさを損ないやすい。

---

## 11. 音

Web Audio APIで単純なサイン波を生成しており、外部音源ファイルは使用しない。

- 返球：速度に応じた短音
- プレイヤー得点：高音
- CPU得点：低音
- 開始／再開：短い確認音

ブラウザで音声が拒否されてもゲームは継続する。

---

## 12. セキュリティとデータ

- 外部APIなし
- APIキーなし
- ログインなし
- データベースなし
- 個人情報収集なし
- 広告・アクセス解析なし
- サーバーへのスコア保存なし

クライアント側のゲームなので、公開後はソースが閲覧可能であることを前提とする。
将来オンラインランキング、アカウント、分析ツールなどを追加する場合は、
セキュリティとプライバシー設計を改めて行うこと。

---

## 13. 開発・確認

Node.js 22.13以上を使用する。

```bash
npm install
npm run dev
```

静的な検査：

```bash
npm run lint
```

ビルド：

```bash
npm run build
```

修正後は、デスクトップだけでなくスマートフォン実機で必ず確認する。

### 実機確認項目

1. 指の動きとバーの追従が自然か
2. 指でバーやボールが隠れないか
3. 縦画面に全体が収まるか
4. 画面スクロールが誤作動しないか
5. 得点後のタップ再開が確実か
6. バーの端や移動打ちで軌道差が感じられるか
7. 加速が急すぎないか
8. 当たり判定が見た目より厳しくないか

---

## 14. GitHubと公開先

このリポジトリはGitHubでの履歴管理や他AIへの受け渡しに利用できる。

現在の構成は静的なVite React版であり、GitHub Pagesへ公開できる。GitHubのSettingsで
PagesのSourceをGitHub Actionsに設定し、`main`ブランチへpushすると同梱のワークフローが
`dist`を自動公開する。別の静的ホスティングでも`npm run build`後の`dist`を利用できる。

ゲームロジックは`app/page.tsx`、見た目は`app/globals.css`を基準とし、公開方法の変更時にも
挙動を勝手に再設計しないこと。

---

## 15. 変更時の原則

1. ユーザーが求めた変更だけを実装する
2. ミニマムさを維持する
3. 操作感を機能数より優先する
4. 物理、色、CPUを同時に大きく変更しない
5. スマートフォン実機で評価する
6. 正常版を履歴として残す
7. 不具合修正時は原因と変更点を引き継ぐ

---

## 16. 他AIへの依頼文

別のAIへ渡す場合は、本リポジトリとともに次を伝える。

> このリポジトリはスマートフォン向け円周ゲーム「Orbit Pong」の完成版です。最初に `ORBIT_PONG_AI_HANDOFF.md` を読み、現在の操作感、ゲームバランス、レスポンシブ設計を理解してください。私は非エンジニアなので、コード断片を提示して手作業を求めるのではなく、依頼した変更をファイルへ直接反映し、可能な範囲で検証してください。特にスマートフォンの横スワイプが円弧上の滑らかなバー移動へ変換される感触と、ゲームのミニマムさを維持してください。
